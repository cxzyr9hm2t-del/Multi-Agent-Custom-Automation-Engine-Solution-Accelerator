[project]
name = "frontend-react"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]>=0.38.0",
    "jinja2",
    "azure-identity",
    "python-dotenv==1.2.2",
    "python-multipart==0.0.32",
    "urllib3==2.7.0",
    # Transitive pins held for security, aligned with src/backend and src/mcp_server.
    # cryptography: GHSA-g6cj-pr64-35w5 and GHSA-jwv3-5hgf-82ww require
    # 50.0.0 / 49.0.0; 50.0.0 also covers the earlier GHSA-537c-gmf6-5ccf.
    "cryptography==50.0.0",
    "starlette==1.3.1",
]
