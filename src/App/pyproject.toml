[project]
name = "frontend-react"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]>=0.38.0",
    "jinja2",
    "azure-identity",
    "python-dotenv==1.2.2",
    "python-multipart==0.0.32",
    "urllib3==2.7.0",
    # Transitive pins held for security, aligned with src/backend and src/mcp_server.
    # cryptography: GHSA-537c-gmf6-5ccf. 48.0.1 is the minimum fixed release —
    # 49.x is a major bump that drops older Python/OpenSSL support.
    "cryptography==48.0.1",
    "starlette==1.3.1",
]
